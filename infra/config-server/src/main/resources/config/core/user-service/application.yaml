spring:
    jpa:
      hibernate:
         ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    sql:
      init:
        mode: always
    datasource:
      driverClassName: org.postgresql.Driver
      url: jdbc:postgresql://192.168.0.45:5432/userdb
      username: postgres
      password: password

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG

server:
  port: 0